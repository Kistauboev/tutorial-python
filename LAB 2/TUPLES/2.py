thistuple = ("apple",)   # if we want to clarify that is tupple, we have to write ',' at the end, otherise it return the str
print(type(thistuple))

#NOT a tuple
thistuple = ("apple")
print(type(thistuple))
